mutation createPost($message: String!, $links: [LinkInput!]) {
    createPost(
        message: $message
        links: $links
    ) {
        _id
        creatorID
        createdTime
        message
        updatedTime
        links
        shares
    }
}
