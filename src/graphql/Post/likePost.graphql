mutation likePost($postID: String!) {
    likePost(postID: $postID) {
        _id
        creatorID
        creatorFullName
        createdTime
        message
        updatedTime
        links
        shares
        likes
    }
}
